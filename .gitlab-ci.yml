stages:
  - spawn

pdf:
  stage: spawn
  trigger:
    include: ci/pdf.yml
  only:
    changes:
      - docs/**/*

ann:
  stage: spawn
  trigger:
    include: ci/ann.yml
  only:
    changes:
      - ai/**/*
      - game/**/*
game:
  stage: spawn
  trigger:
    include: ci/game.yml
  only:
    changes:
      - ai/**/*
      - game/**/*